apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    name: myapp-pod-monitor
spec:
  containers:
  - name: prometheus
    image: ubuntu/prometheus
  - name: grafana
    image: grafana/grafana